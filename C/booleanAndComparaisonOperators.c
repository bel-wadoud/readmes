#include <stdio.h>
#include <stdbool.h>

int main() {
  bool valid = true;
  bool invalid = false;

  /* the comparasion operators are:
   * > greater than
   * < smaller than
   * == equal to 
   * != not equal to 
   * >= greater or equal
   * <= smaller or equal
   */

  /* logical operators are:
   * && and
   * || or 
   * ! not
   */

  printf("%d %d \n", valid, invalid); // true and false are represented with 0 1
}
