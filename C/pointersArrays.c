#include <stdio.h>

int main () {
  int arr[5] = {1, 4, 9, 11, 0};
  
  // arr --> address of the first element
  // *arr --> the value of the first element
  // arr + i --> address of the ith element.
  // *(arr + i) --> value of the ith element.
  // &arr --> address of the whole array (1st element)


  return 0;
}
