#include <stdio.h>

int main() {
  // structure
  /*
  if (condition) {
    code
  } else if (condition) {

  } else {

  }
  */

  // Ternary Operator (short if)
  // test_condition ? expression1 : expression2;
  return 0;
}
