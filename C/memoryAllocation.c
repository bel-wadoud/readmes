#include <stdio.h>

// NEED TO BE EDITED LATER

// structure
// int n;
// int* ptr;
// ptr = (int*) malloc(n * sizeof(int));
//
// free(ptr); // to free up the allocated space
// ptr = realloc(ptr, sizeof(n * int)); // reallocates new memory space.


