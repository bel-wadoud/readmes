/*
 *
 * create::path::to::file ==> absolute path
 * path::to::file ==> relative path
 *
 * super:: ==> means ../
 */

use std::fmt::Result;
use std::io::Result as IoResult;

use rand::Rng; // cargo add rand.

// use std::cmp::Ordering;
// use std::io;

use std::{cmp::Ordering, io};

// use::std::io;
// use::std::io::Write;

use std::io::{self, Write};

fn main() {
    println!("Hello world");
}
