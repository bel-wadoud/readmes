/*
mod vectors;
use vectors::main as vecs;
*/

/*
mod strings;
use strings::main as strs;
*/

/*
mod hashmaps;
use hashmaps::main as hashs;
*/

/*
mod errors;
use errors::main as errrs;
*/

/*
mod generics;
use generics::main as gens;
*/

mod traits;
use traits::main as traits;

fn main() {
    // vecs();
    // strs();
    // hashs();
    // errrs();
    // gens();
    traits();
}
